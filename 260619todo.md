# 260619 시나리오 파일 캐싱 TODO

## 현재 구현

ScenarioDetailView `onMounted`에서 플레이 가능 여부 확인 후 `game.preloadScenario(scId)` 호출.
`gameStore.preloadScenario(scId)` → `_loadScenarioFiles(scId)` → `_preloadedScId / _preloadedData`에 저장.  
`startGame` 호출 시 `_preloadedScId === scId`이면 재요청 없이 캐시 사용.

## 로드 대상 파일 (6개)

```
SE796/0211/010/
  starDetail.js          → STAR_DETAIL
  characters/
    charactersJobs.js    → CHAR_JOBS
  fleet/
    fleetData.js         → FLEET_DATA
    fleetCharacterData.js→ FLEET_CHARACTER_DATA
    fleetShipData.js     → FLEET_SHIP_DATA
    fleetTraitData.js    → FLEET_TRAIT_DATA
```

## 검토 항목

- [ ] `charactersData.js` (CHAR_LIST) — ScenarioCharSelectView에서 별도 동적 import 중.  
  프리로드에 포함시킬지 여부 결정
- [ ] `scenarioDesc.js` (DESC) — 이미 `scenarioData.js` 정적 import로 포함됨. 중복 로드 없음 확인
- [ ] `planetDetail.js` — 현재 로드 목록에 없음. 필요 여부 확인
- [ ] variant 전환 시 (◁▷) 캐시 처리 — 다른 scId로 전환하면 캐시 갱신 필요

### 
ScenarioDetailView `onMounted`에서 플레이 가능 여부 확인 후 `game.preloadScenario(scId)` 호출.
호출 시작에는 기존 preloadScenario를 초기화.

호출 중엔 버튼이 비활성화 및 로딩 표시 들어가야함. 좌에서 우로 게이지 채우고, 어떤 데이터를 불러오는지 처리하면 됨.
(그 동안 유져들은 해당 시나리오의 배경을 보면 됨.)

1. base만 참조하면 되는 불변 데이터 세팅


1. `src/data/base/stars/starSystemData.js` (STAR_SYSTEMS) 조회
   > `SE796/0211/010/stars/starSystemData.js` (시나리오의 STAR_SYSTEM) 조회
      → 게임 내 실제 성계 상태 완성
