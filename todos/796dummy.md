# SE796_0211_010 — 아스타테 성역 회전 「영원한 어둠 속에서」

> 시나리오 개요 / 진입 흐름 / 초기 함대·인물 데이터는 검증 완료 후 `docs/`로 이관됨:
> [103_data_scenarios.md](../docs/103_data_scenarios.md) (폴더 구조, 인물 직책 오버라이드),
> [106_data_fleet.md](../docs/106_data_fleet.md) (SE796_0211_010 편성 표),
> [205_screen_scenario.md](../docs/205_screen_scenario.md) (화면 흐름, libs 필드 실사용 예).
> 아래는 이 시나리오에서 **아직 코드로 구현되지 않은** 연출/이벤트/승리조건 설계만 남긴 것.

---

## 1. 게임 진입 시 발생해야 할 이벤트

### 1-1, 1-2. 아스타테 진입 배치 + 제2함대 조우 — ✅ 구현 완료 (2026-07-05)
- **결정**: 게임 시작과 동시에 함대가 이미 아스타테 성역에 배치된 상태로 시작 (의안 자동등록 방식 대신 채택)
- `fleetData.js`의 FPA002(제2함대) 시작 위치를 `230006`(하이네센) → `230005`(아스타테)로 변경
- `gameStore._checkInitialEncounters()` 신규 — `startGame()` 직후 호출, playerFaction 소속 함대와 같은 성계에 있는
  적 함대를 찾아 `_pendingBattles`에 즉시 등록 (시나리오 무관 범용 로직, 데이터만으로 재사용 가능)
- `GameView.vue`의 `_pendingBattles` watcher에 `{ immediate: true }` 추가 — 기존에는 컴포넌트 마운트 **이전**에
  이미 채워진 큐를 감지하지 못하는 타이밍 버그가 있었음 (일반 `watch`는 값이 "변경"될 때만 발화하므로).
  `ctx._notified` 플래그로 중복 확인창 방지.
- Playwright로 검증: REH(라인하르트)로 시작 → 플레이어가 교전 함대 소속이므로 확인창 없이 곧장 전술뷰 진입,
  REH 24,000척 vs FPA 15,000척 정상 표시. PZN(비참여 세력)으로 시작 → 정상적으로 전략화면 진입, 크래시 없음.

### 1-3. 양 웬리 플레이어블 트리거
- FPA 플레이어가 양 웬리를 인물로 선택했을 경우: 제2함대 사령관 권한이 파에타에서 양으로 이전되는 이벤트 연출 필요

---

## 2. 승리/패배 조건 (이 시나리오 전용, 미구현)

현재 코드에서 `_victory()`는 70% 성계 점유 일반 조건만 존재. 아스타테 시나리오 전용 조건은 추가 구현 필요.

| 진영 | 승리 조건 (안) |
|---|---|
| REH | 동맹 3개 분함대 중 2개 이상 격파 또는 아스타테 성역 점령 |
| FPA | 라인하르트 함대 격퇴 또는 로엔그람 기함 격파 |
| PZN | 양 진영 모두 소진될 때까지 중립 유지 후 교역 수입 X턴 달성 |

---

## 3. 남은 컨텐츠/데이터 이슈

- **`scenarioDesc.js` 인터페이스 미사용 필드**: 명시된 페이지 인터페이스는 `{ index, image/bg, char, charName, text, effect, libs }`이나
  실제 3페이지 모두 `char`/`charName`을 채우지 않음 — 코드 버그는 아니고 연출 콘텐츠 미완성. (`gap_analysis.md` 트래킹)

---

## 4. 미결 사항 (TODO)

- [x] FPA003(르페브르) charCode 등록 — 2026-07-05
- [x] REH 함대 초기 locCode 확정 (아스타테 230005) + `buildFleetsMap()` 분함대 합산 로직 — 2026-07-04
- [x] 오프닝 내레이션 인명 수정 (패트릭 캐슬네스 → 파스톨레) — 2026-07-05
- [x] 분함대 사령관(메르카츠 등)을 REH004에 반영 — `subCommanders` 필드 추가 (officers와 구분) — 2026-07-05
- [x] `REH001`(뮈켄베르거 함대) / 이제르론 주둔함대(미터마이어/로이엔탈) — **이 시나리오에는 추가하지 않기로 결정 (2026-07-05)**.
  둘 다 아스타테 회전에 직접 참여하지 않는 후방/주둔 함대이며, 다른 시나리오(예: 이제르론 관련)에 등장할 때 추가하는 것으로 범위 확정.
- [x] 1턴 시작 시 강제 전술턴 진입 로직 — "이미 배치된 상태로 시작" 채택, `_checkInitialEncounters()` 구현 — 2026-07-05 (위 1-1/1-2절)
- [ ] 시나리오 전용 승리 조건 구현 (위 2절)
- [ ] 양 웬리 플레이어블 시 제2함대 지휘권 이전 이벤트 (위 1-3절)
