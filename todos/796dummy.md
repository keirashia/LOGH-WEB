# SE796_0211_010 — 아스타테 성역 회전 「영원한 어둠 속에서」

> 시나리오 개요 / 진입 흐름 / 초기 함대·인물 데이터는 검증 완료 후 `docs/`로 이관됨:
> [103_data_scenarios.md](../docs/103_data_scenarios.md) (폴더 구조, 인물 직책 오버라이드),
> [106_data_fleet.md](../docs/106_data_fleet.md) (SE796_0211_010 편성 표),
> [205_screen_scenario.md](../docs/205_screen_scenario.md) (화면 흐름, libs 필드 실사용 예).
> 아래는 이 시나리오에서 **아직 코드로 구현되지 않은** 연출/이벤트/승리조건 설계만 남긴 것.

---

## 1. 게임 진입 시 발생해야 할 이벤트 (미구현)

### 1-1. 아스타테 진입 직전 포위망 전술 이벤트
- 게임 시작 직후(1턴 개시 전), 동맹 3개 분함대가 아스타테 성역 방면으로 이동 명령이 의안으로 자동 등록되어 있는 상태로 시작
- 또는: 게임 시작과 동시에 함대가 이미 아스타테 성역에 배치된 상태로 시작 (구현 방향 결정 필요)

### 1-2. 제2분함대 조우 이벤트 (1턴)
- 제2분함대(파에타)와 로엔그람 함대의 조우 → 전술턴 자동 진입 or 자동 처리 선택지

### 1-3. 양 웬리 플레이어블 트리거
- FPA 플레이어가 양 웬리를 인물로 선택했을 경우: 제2함대 사령관 권한이 파에타에서 양으로 이전되는 이벤트 연출 필요

---

## 2. 승리/패배 조건 (이 시나리오 전용, 미구현)

현재 코드에서 `_victory()`는 70% 성계 점유 일반 조건만 존재. 아스타테 시나리오 전용 조건은 추가 구현 필요.

| 진영 | 승리 조건 (안) |
|---|---|
| REH | 동맹 3개 분함대 중 2개 이상 격파 또는 아스타테 성역 점령 |
| FPA | 라인하르트 함대 격퇴 또는 로엔그람 기함 격파 |
| PZN | 양 진영 모두 소진될 때까지 중립 유지 후 교역 수입 X턴 달성 |

---

## 3. 남은 컨텐츠/데이터 이슈

- **오프닝 내레이션과 함대 데이터 간 인명 불일치**: `scenarioDesc.js` 3페이지 텍스트는 "제4함대 사령관 패트릭 캐슬네스 중장"이
  후퇴를 결단했다고 서술하지만, 실제 `FPA004` 사령관은 파스톨레(`CH_000478`)다. 오프닝 컷씬 스크립트 쪽 인명 수정 필요.
  (`gap_analysis.md` 트래킹)
- **`scenarioDesc.js` 인터페이스 미사용 필드**: 명시된 페이지 인터페이스는 `{ index, image/bg, char, charName, text, effect, libs }`이나
  실제 3페이지 모두 `char`/`charName`을 채우지 않음 — 코드 버그는 아니고 연출 콘텐츠 미완성. (`gap_analysis.md` 트래킹)

---

## 4. 미결 사항 (TODO)

- [x] FPA003(르페브르) charCode 등록 — 2026-07-05
- [x] REH 함대 초기 locCode 확정 (아스타테 230005) + `buildFleetsMap()` 분함대 합산 로직 — 2026-07-04
- [ ] `REH001`(뮈켄베르거 함대) 데이터 추가, 이제르론 주둔 제국 후방 함대(미터마이어/로이엔탈) 데이터 추가
- [ ] 분함대 사령관(메르카츠 등)을 REH004 `officers`에 반영 (현재는 함선 수만 합산됨)
- [ ] 시나리오 전용 승리 조건 구현 (위 2절)
- [ ] 1턴 시작 시 강제 전술턴 진입 로직 또는 함대 이미 교전 배치 로직 결정 (위 1-1/1-2절)
- [ ] 양 웬리 플레이어블 시 제2함대 지휘권 이전 이벤트 (위 1-3절)
